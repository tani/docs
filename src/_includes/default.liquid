<!doctype html>
<html lang="{{ lang }}">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}} - docs.casa</title>
  <link rel="icon" href="data:," />
  <link rel="preconnect" href="https://fonts.googleapis.com/" crossorigin>
  <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin>
  <link rel="preconnect" href="https://cdn.jsdelivr.net.com/" crossorigin>
  <script async src="https://cdn.jsdelivr.net/npm/bootstrap@5/dist/js/bootstrap.bundle.min.js"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootswatch@5/dist/sandstone/bootstrap.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1/font/bootstrap-icons.min.css" />
  <style>
    aside h2 {
      font-size: 1.3em;
    }
    input.tag~ul {
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.25s;
    }

    input.tag:checked~ul {
      max-height: 500px;
    }

    label.tag {
      cursor: pointer;
    }
  </style>
</head>

<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">M.TANIGUCHI</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
          aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/profile/"><i class="bi bi-person-circle"></i> Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="https://github.com/tani"><i class="bi bi-github"></i> GitHub</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <main>
    <div class="container py-3 border-bottom">
      <div class="row">
        <div class="col-md-9">
          <article>
            {{ content | safe }}
            (Published at {{ page.date | toUTCString }})
          </article>
        </div>
        <div class="col-md-3">
          <aside class="sticky-top">
            <div class="d-none d-md-block">
              <h2>Table of Contents</h2>
              {{ content | toc }}
            </div>
            <div class="pt-3 pt-md-0">
              <h2>See also</h2>
              <ul>
                {% for tag in collections.tagList %}
                <li>
                  <label for="{{ tag }}" class="tag"><code>{{ tag }}</code></label>
                  <input id="{{ tag }}" class="tag" type="checkbox" hidden />
                  <ul>
                    {% for article in collections[tag]%}
                    <li><a href="{{ article.url }}">{{ article.data.title }}</a></li>
                    {% endfor %}
                  </ul>
                </li>
                {% endfor %}
              </ul>
            </div>
          </aside>
        </div>
      </div>
    </div>
  </main>
  <footer>
    <div class="container py-3">
      <div class="row justify-content-center">
        <div class="col-xl-10">
          <p>Copyright (c) 2019-2021 TANIGUCHI Masaya All Rights Reserved.</p>
          <p>Please tell me on <a href="https://github.com/tani/docs">GitHub</a> if you found any typos and
            misstatements.</p>
        </div>
      </div>
    </div>
  </footer>
</body>

</html>
